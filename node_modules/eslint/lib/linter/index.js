"use strict";

const {Linter} = require("eslint/lib/linter/linter");
const interpolate = require("eslint/lib/linter/interpolate");
const SourceCodeFixer = require("eslint/lib/linter/source-code-fixer");

module.exports = {
    Linter,

    // For testers.
    SourceCodeFixer,
    interpolate
};
