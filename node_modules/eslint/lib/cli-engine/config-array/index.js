/**
 * @fileoverview `ConfigArray` class.
 * @author Toru Nagashima <https://github.com/mysticatea>
 */
"use strict";

const {ConfigArray, getUsedExtractedConfigs} = require("eslint/lib/cli-engine/config-array/config-array");
const {ConfigDependency} = require("eslint/lib/cli-engine/config-array/config-dependency");
const {ExtractedConfig} = require("eslint/lib/cli-engine/config-array/extracted-config");
const {IgnorePattern} = require("eslint/lib/cli-engine/config-array/ignore-pattern");
const {OverrideTester} = require("eslint/lib/cli-engine/config-array/override-tester");

module.exports = {
    ConfigArray,
    ConfigDependency,
    ExtractedConfig,
    IgnorePattern,
    OverrideTester,
    getUsedExtractedConfigs
};
