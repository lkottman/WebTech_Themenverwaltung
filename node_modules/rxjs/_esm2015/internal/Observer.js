import {config} from 'rxjs/_esm2015/internal/config';
import {hostReportError} from 'rxjs/_esm2015/internal/util/hostReportError';

export const empty = {
    closed: true,
    next(value) {
    },
    error(err) {
        if (config.useDeprecatedSynchronousErrorHandling) {
            throw err;
        } else {
            hostReportError(err);
        }
    },
    complete() {
    }
};
//# sourceMappingURL=Observer.js.map