import {isArray} from 'rxjs/_esm2015/internal/util/isArray';
import {CombineLatestOperator} from 'rxjs/_esm2015/internal/observable/combineLatest';
import {from} from 'rxjs/_esm2015/internal/observable/from';

const none = {};

export function combineLatest(...observables) {
    let project = null;
    if (typeof observables[observables.length - 1] === 'function') {
        project = observables.pop();
    }
    if (observables.length === 1 && isArray(observables[0])) {
        observables = observables[0].slice();
    }
    return (source) => source.lift.call(from([source, ...observables]), new CombineLatestOperator(project));
}

//# sourceMappingURL=combineLatest.js.map