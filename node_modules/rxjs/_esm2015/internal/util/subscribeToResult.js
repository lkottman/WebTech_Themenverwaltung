import {InnerSubscriber} from 'rxjs/_esm2015/internal/InnerSubscriber';
import {subscribeTo} from 'rxjs/_esm2015/internal/util/subscribeTo';
import {Observable} from 'rxjs/_esm2015/internal/Observable';

export function subscribeToResult(outerSubscriber, result, outerValue, outerIndex, innerSubscriber = new InnerSubscriber(outerSubscriber, outerValue, outerIndex)) {
    if (innerSubscriber.closed) {
        return undefined;
    }
    if (result instanceof Observable) {
        return result.subscribe(innerSubscriber);
    }
    return subscribeTo(result)(innerSubscriber);
}

//# sourceMappingURL=subscribeToResult.js.map