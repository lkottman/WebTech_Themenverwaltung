import {isScheduler} from 'rxjs/_esm2015/internal/util/isScheduler';
import {fromArray} from 'rxjs/_esm2015/internal/observable/fromArray';
import {scheduleArray} from 'rxjs/_esm2015/internal/scheduled/scheduleArray';

export function of(...args) {
    let scheduler = args[args.length - 1];
    if (isScheduler(scheduler)) {
        args.pop();
        return scheduleArray(args, scheduler);
    } else {
        return fromArray(args);
    }
}

//# sourceMappingURL=of.js.map